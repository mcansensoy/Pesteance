using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class UpRunState : PlayerState
{
    public UpRunState(Player _player, PlayerStateMachine _stateMachine, string _animBoolName) : base(_player, _stateMachine, _animBoolName)
    {
    }

    public override void Enter()
    {
        base.Enter();
    }

    public override void Exit()
    {
        base.Exit();
    }

    public override void Update()
    {
        base.Update();
        rb.MovePosition(rb.position + moveInput * (player.moveSpeed * Time.fixedDeltaTime));

        if(moveInput == player.xd)
            stateMachine.ChangeState(player.upIdleState);
    }
}*/
